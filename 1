---
  - name: Test the module
    hosts: localhost
    become: yes
    tasks:
      - name: Just the name
        chage:
          name: chager
          set_amount_days: '100'
        no_log: True
        register: output
      - debug:
          msg: "{{ output }}"
