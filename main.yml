---
  - name: Test the module
    hosts: localhost
    become: yes
    tasks:
      - name: Just the name
        my_test:
          name: chager
          set_amount_days: '100'
        register: output
      - debug:
          msg: "{{ output }}"
