---
  - name: Test the module
    hosts: localhost
    become: yes
    tasks:
      - name: Just the name
        my_test:
          name: chager
          pass_expires: True
          last_pass_change: True
        register: output
      - debug:
          msg: "{{ output }}"
